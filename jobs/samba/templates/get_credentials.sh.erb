#!/bin/bash
PASSD="/home/vcap/.passwd"
USERNAME=<%= p("smb.username") %>
IPADDRESS=$(ip addr | grep 'state UP' -A2 | tail -n1 | awk '{print $2}' | cut -f1  -d'/')

echo "$USERNAME"
echo "$(sudo cat $PASSD)"
echo $IPADDRESS